{
  "name": "@onp/badge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Embeddable <onp-badge> web component: verifies an ONP News Object in the reader's browser and shows a provenance badge. Consumes the published open-news-protocol SDK.",
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/test/*.test.js",
    "bundle": "esbuild src/onp-badge.ts --bundle --format=esm --minify --outfile=dist/onp-badge.js --platform=browser"
  },
  "dependencies": {
    "open-news-protocol": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
